# LAB01_DETAILED_EXPLANATION

## Phần 1: wc (Word Count)
Chương trình `wc` được sử dụng để đếm số từ, dòng và ký tự trong một tập tin. Dưới đây là phần giải thích và mã nguồn chi tiết.

### Giải thích
- `wc` đọc dữ liệu từ tập tin đầu vào và phân tích nó để xem có bao nhiêu dòng, từ và ký tự.
  
### Mã nguồn
```c
// Mã chương trình wc ở đây
```

## Phần 2: cat (Concatenate and Display)
Chương trình `cat` được sử dụng để nối và hiển thị nội dung của một hoặc nhiều tập tin.

### Giải thích
- `cat` sử dụng để xem nội dung của tập tin một cách dễ dàng mà không cần mở trình soạn thảo.

### Mã nguồn
```c
// Mã chương trình cat ở đây
```

## Phần 3: getprocs (Get Processes)
Chương trình `getprocs` trả về thông tin về các tiến trình đang chạy trong hệ thống.

### Giải thích
- `getprocs` có thể được sử dụng để theo dõi các tiến trình và trạng thái của chúng.

### Mã nguồn
```c
// Mã chương trình getprocs ở đây
```

## Phần 4: trace (Trace System Calls)
Chương trình `trace` theo dõi các cuộc gọi hệ thống của một tiến trình.

### Giải thích
- `trace` giúp hiểu rõ hơn về cách mà các tiến trình tương tác với hệ thống.

### Mã nguồn
```c
// Mã chương trình trace ở đây
```